extends ../layout 

block content
    header.join-header
        h1 #{title}
    br
    if(article)
        ul.list-group()
            each article, i in articles 
                li.list-group-item.color-container
                    a.page-link(href="/articles/"+article._id)= article.title
    else 
        p.alert-undefined No article wrotten.